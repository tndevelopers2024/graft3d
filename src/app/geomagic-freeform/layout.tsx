import { PAGE_METADATA } from '@/lib/metadata';

export const metadata = PAGE_METADATA.geomagicFreeform;

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
